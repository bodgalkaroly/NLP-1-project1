[project]
name = "text-analysis-env"
version = "0.1.0"
description = "Environment for text and data analysis with nltk, sklearn, scipy, and matplotlib"
requires-python = ">=3.12"

dependencies = [
    "matplotlib",
    "numpy",
    "scikit-learn",
    "tqdm",
    "nltk",
    "scipy",
]

[tool.uv]